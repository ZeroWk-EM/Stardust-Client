import Endpoint from "../Endpoint/Endpoint";
import Title from "../Title/Title";

const Home = () => {
  return (
    <div>
      <Title />
      <Endpoint />
    </div>
  );
};

export default Home;
